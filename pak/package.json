{
  "name": "pak",
  "version": "0.1.0",
  "description": "Unreal Engine .pak reader based on repak",
  "main": "index.js",
  "scripts": {
    "build": "cargo-cp-artifact -nc index.node -- cargo build --message-format=json-render-diagnostics",
    "install": "npm run build",
    "test": "cargo test"
  },
  "license": "MIT",
  "devDependencies": {
    "cargo-cp-artifact": "^0.1"
  }
}
